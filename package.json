{
  "name": "playwright-parabank",
  "version": "1.0.0",
  "description": "Playwright conversion of ParaBank Selenium framework",
  "scripts": {
    "test": "playwright test",
    "bdd": "cucumber-js --require-module ts-node/register --require step-definitions/**/*.ts features/**/*.feature"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@cucumber/cucumber": "^9.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.4.0"
  }
}